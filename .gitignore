# =============================================================================
# PA Agent — 仅上传源代码与项目文档；本地运行数据、密钥、临时文件不入库
# =============================================================================

# ── Python / 构建产物 ─────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.hypothesis/

# ── 密钥与本地配置（切勿提交）────────────────────────────────────────────────
.env
.env.*
config/settings.json
config/settings.local.json
config/exception_state.json
config/secret.key
config/tv_symbol_aliases.json
config/feishu.json
*.pem
*.key

# ── 运行时数据（分析记录、经验库、日志、交易导出）────────────────────────────
logs/*
!logs/.gitkeep

records/**/*
!records/**/.gitkeep

experience/**/*
!experience/**/.gitkeep

trade_records/*
!trade_records/.gitkeep

# ── 本地临时 / 解包产物（WorkBuddy、网关调试等）──────────────────────────────
_tmp_wb_asar/
_tmp_auth.js
auth.js
index.js

# ── 根目录杂项：个人笔记、封面图、一次性脚本 ─────────────────────────────────
/*.jpeg
/*.jpg
/*.png
!/赞助码.jpeg
!/assets/**/*.png
videoCover-*
*_fix_*.md
remove_*.md
tasks.json
fix_garbage_line.py
分析*.md
MAC*.txt
*部署*.txt
*智能体*.txt
# 以下文件已迁移至 docs/ 子目录，根目录不再保留
# （如需在 docs/ 下保留特定文件，请在 docs/ 段单独加 ! 规则）

# ── 根目录临时测试脚本（调试产物，用完即删）─────────────────────────────────
/test_debug*.py
/test_redesign*.py
/test_kline_button*.py
/test_bindEvents*.py
/test_bug_diagnose*.py
/test_redesign_verify*.py
/test_sse*.py
/extract_tv_token.py
/screenshot*.py
/diag_*.py
/diag_screenshots/
/web/static/extract-token.html

# ── IDE / Agent 本地目录 ─────────────────────────────────────────────────────
.idea/
.vscode/
.cursor/
.kiro/
.trae/

# ── 临时调试产物（不入库）────────────────────────────────────────────────────
.tmp_record*.json
.tmp_*.json

# ── tools 目录下的本地诊断输出（保留 .py 脚本本身）──────────────────────────
tools/_pending_order_report.txt
tools/stage2_raw_sample.txt
tools/_tools_catalog.json
tools/_diag_*.txt
# 根目录探针产物：tools/probe_thinking_routes.py 的运行输出（一次性诊断，不入库）
/thinking_route_probe_result.json

# ── 个人开发笔记（非对外文档）────────────────────────────────────────────────
docs/changelog/

# ── OS ───────────────────────────────────────────────────────────────────────
Thumbs.db
Desktop.ini
.DS_Store
