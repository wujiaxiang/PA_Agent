services:
  pa-agent-web:
    build:
      context: ..
      dockerfile: web/Dockerfile
    ports:
      - "8005:8000"
    volumes:
      - ../config:/app/config
      - ../records:/app/records
      - ../logs:/app/logs
    restart: unless-stopped
